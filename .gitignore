# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
node_modules
.next
.env.local
node_modules
.next
.env.local
node_modules
.next
.env.local
node_modules
.next
.env.local
node_modules
.next
.env.local
.next
.env.local
.next
.env.local
.env
.env.local
.env
.env.local
.env
.env*.backup
.env*.backup
.env*.backup
.env*.backup
.env*.backup
.env*.backup
.env*.backup

# Script outputs & audit logs (never commit these — may contain PII / question data)
logs/
scripts/output/
*.audit.csv
*.audit.json

# Exam paper PDFs — large binaries (217MB+), stored separately, not in git
exam-papers-staging/

# Dev / design prototypes — HTML mockups, not part of the app
public/boss-preview.html
public/test-3d-quiz-visuals.html
public/test-avatar-colors.html
science-simulations-boss-battles.html
science-simulations-boss-battles-v2.html
stellar-command-dashboard.html
avatar-concept-prototype.html
FREE_ASSET_CATALOGUE.html

# Generated / cached audio files — can be regenerated, not source code
public/audio/

# Marketing & outreach assets — not part of the codebase
launchpard-beta-invite.png
whatsapp-promos-and-beta-invite.md

# One-off utility scripts not part of the main project
trigger-nudge.mjs
